# Clicks

이름으로 하여 클릭한 횟수를 세어 랭킹을 작성합니다.

## 사용법

`json-server`를 이용하여 다음 템플릿으로 서버를 실행합니다.

```json
{
  "clicks": []
}
```

서버의 주소는 `localhost:3001`로 설정하여 실행합니다.

이후, `npm build` 명령어를 통하여 웹페이지를 빌드한 다음
`build` 디렉토리에서 `npx serve` 또는 `python3 -m http.server 3000`
등의 명령어를 사용하여 서버를 실행합니다.
